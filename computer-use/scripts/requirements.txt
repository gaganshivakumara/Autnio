# Anthropic Computer Use agent — local dependencies
# Install with: pip install -r computer-use/scripts/requirements.txt

# AWS SDK (Bedrock computer use loop)
boto3>=1.34

# WebSocket client (run-agent.py relay loop)
websockets>=12,<14

# Computer control — mouse, keyboard, screenshot
pyautogui>=0.9.54
Pillow>=10.0

# Clipboard support for Unicode text input
pyperclip>=1.8

# Agent HTTP server (agent-server.py — browser relay path)
fastapi>=0.111
uvicorn[standard]>=0.29

# Voice I/O — local mic capture + TTS playback
amazon-transcribe>=0.6.4
sounddevice>=0.4.6
numpy>=1.24
pygame>=2.5
