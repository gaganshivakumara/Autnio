export function FileBrowser(): JSX.Element {
  return (
    <section className="card">
      <h2>Files</h2>
      <p>
        Box file browsing depends on Dev2 `box-read`, `box-write`, and `box-share`
        Lambda ARNs. This panel is reserved for that integration.
      </p>
    </section>
  );
}
